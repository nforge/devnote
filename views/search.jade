extends layout

block content
  div.page-header
    h1= __('Searching result')

  form.well
    input(name="keyword").search-query
    input(type="submit", name="action", value=__('Search')).btn
  ul
    each content, name in pages
      li
        a(href="/wikis/note/pages/"+name)= name
        p!= content
