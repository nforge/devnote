extends layout

block content
  div.page-header
    h1= __('Searching result')

  ul.result
    each rendered, name in pages
      li
        p.title: a(href="/wikis/#{encodeURIComponent(wikiName)}/pages/#{encodeURIComponent(name)}")!= rendered.title
        p.text!= rendered.body
